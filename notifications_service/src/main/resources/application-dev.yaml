spring:
  docker:
    compose:
      enabled: true
      file:
        - docker_build/dev/docker-compose.yaml

server:
  port: 8003

messaging:
  notifications_queue: notifications_queue

inbox:
  reservation_in_minutes: 5
  polling:
    to_process_by_time:
      number: 10