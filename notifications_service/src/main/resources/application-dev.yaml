spring:
  docker:
    compose:
      enabled: true
      file:
        - docker_build/dev/docker-compose.yaml
  jpa:
    show-sql: true
  mail:
    host: smtp.gmail.com
    username: ${SMTP_USER}
    password: ${SMTP_PASS}
    port: 587
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          connectiontimeout: 5000
          timeout: 10000
          writetimeout: 10000
  datasource:
    username: developer
    password: developer
    url: jdbc:postgresql://localhost:5432/notifications_db

logging:
  level:
    com.supvz.notifications_service: trace

# noinspection undefined
app:
  notification:
    thread:
      core-pool-size: 20
      max-pool-size: 50
      queue-capacity: 50
#     Capacity должна превышать batch-size. В два раза - дефолт.
      name-prefix: notification
      wait-tasks-shutdown: true
      await-termination-sec: 60
    number-retry-attempts: 2
  websocket:
    base-topic: topic
    allowed-origins:
      - "*"
  cors:
    allowed-origins:
      - "*"
    allowed-methods:
      - "GET"
      - "POST"
      - "PUT"
      - "DELETE"
      - "OPTIONS"
  messaging:
    inbox-queue: inbox-queue
  inbox:
    reservation-min: 1
    cleaning-min: 1
    polling:
      cleaning:
        batch-size: 10
        delay-ms: 1000
      processing:
        batch-size: 20
        delay-ms: 10000