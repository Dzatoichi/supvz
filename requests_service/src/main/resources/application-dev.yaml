spring:
  docker:
    compose:
      enabled: true
      file:
        - docker_build/dev/docker-compose.yaml
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

logging:
  level:
    com.supvz.requests_service: trace